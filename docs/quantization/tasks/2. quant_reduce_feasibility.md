# My Prompt

Consider we are going to do activation quantization, which can accelerate the PE-2-PE communication.

The main question is that, is it possible to reduce the communication for reduce? Becuase the reduce tensor is the partial result or the final result of the op. If we quantize the intermediate result, I suppose it would cause accuracy down.

Please help me deepssearch, search some paper. I also suppose that their some paper that apply quantization to reduce the communication overhead in large-scale distirbuted system.

Just include all the methods. We don't need to implement now. I need a docs / report, addressing my concern: whether it would hurt accuracy. And then analyze the potential speedup and performance gain.

---

# Quantizing the All-Reduce on WSE: Feasibility Analysis Plan

> **Deliverable:** `docs/quantization/docs/2. quant_reduce_feasibility.md` — a literature-grounded analysis report. **No kernel changes** in this task. The report addresses two questions raised by the user: (1) does quantizing the partial sums during the all-reduce hurt model accuracy? (2) what is the projected speedup of doing so on WSE under the constant-per-PE-workload sweep from doc 1?

**Goal:** Produce a single analysis document that (a) resolves the partial-sum-quantization accuracy concern by clearly distinguishing channel-quantization (safe) from accumulator-quantization (compounds error), backed by recent TP-comm-compression papers; (b) catalogs all relevant methods from the literature (channel-only INT8, bcast-only, outlier-aware mixed precision, two-step All2All, microscaling formats, stochastic rounding); (c) projects per-config cycle savings on WSE under a conservative bytes-proportional transit model, applied to doc 1's measured `reduce_mean` / `bcast_mean` numbers; (d) flags WSE-specific feasibility considerations (CSL streaming DSDs, K-tree topology, per-PE compute budget) so a future implementation task can start with realistic constraints.

**Methodology:**

1. **Identify the directly applicable papers** — TP-comm-compression for LLM inference (Apple arXiv:2411.07942; Yao et al. arXiv:2411.09510; Flash Communication arXiv:2412.04964), production-grade quantized AllReduce (EQuARX arXiv:2506.17615), classical gradient compression for context (QSGD arXiv:1610.02132; ECQ-SGD arXiv:1806.08054), formats (OCP MX spec; AMXFP4 arXiv:2411.09909). Quote their accuracy numbers and design choices verbatim where possible.

2. **Resolve the accuracy concern (§2.2 + §2.3 of the report)** — explain why the literature's "channel-only" design pattern (dequantize before every K-tree add, transmit at INT8/INT4, accumulate at FP16) does **not** compound error through K-tree depth. Cite EQuARX's empirical MSE numbers (1.8× speedup at MSE 0.0014 vs naive FP8 at MSE 0.13) and Apple's Irwin-Hall argument (aggregated quantization errors → ≈Gaussian centered at 0 as device count grows). Flag honestly that Apple's argument is intuitive, not formally proved.

3. **Build a method-catalog table (§2.4)** — each row a method, columns: bits, where quantized in the K-tree, accuracy in cited paper, applicability to WSE K-tree topology. Methods in scope: channel-only INT8, bcast-only INT8, outlier-aware INT4+FP16, two-step All2All (flagged as topology-incompatible), MXINT8 / MXFP8, stochastic rounding + error feedback.

4. **Project speedups (§2.5)** — apply a transit-bytes-proportional model to doc 1's measured cycles. Critical: separate the per-PE-local component (reconfig ≈1188 cycles, doesn't scale with bytes) from the K-tree transit-bound component (reduce + bcast minus reconfig). Halve the transit component for INT8, 0.26× it for outlier-aware INT4+BF16. Produce a per-method × per-P table comparable in shape to doc 1's table.

5. **Project accuracy (§2.6)** — restate the per-method numbers from cited papers, note the gap that those papers test 7B–70B models while WaferLLM targets a TinyLlama-class ~1.1B; this gap is *favorable* (smaller models have simpler activation distributions and are typically easier to quantize) but unvalidated.

6. **WSE feasibility notes (§2.7)** — peek at `test/decode_interop/src/comm_lib/comm_pe.csl` lines 491–1032 to confirm the streaming DSD pattern. Identify where dequant must be inserted (between fabric recv DSD and `@faddh`), name the CSL building blocks needed (`@i2f`, `@fmul` per scale, `@f2i_s` for round-to-nearest or stochastic rounding via additional noise channel), and flag the open question of how per-block scale travels alongside the data on the fabric.

7. **Recommend an implementation ordering (§2.8)** — bcast-only first (zero accuracy risk, validates measurement plumbing), then channel-only INT8 (the bulk of the projected win), then outlier-aware INT4 only if INT8 is insufficient. Explicitly *not* implemented in this task.

**Sources (read these before writing):**

- `docs/quantization/docs/1. interop_overhead_analysis.md` — the cycle measurements this analysis projects against.
- `test/decode_interop/src/comm_lib/comm_pe.csl:491-1032` — the actual K-tree implementation; informs the WSE feasibility section.
- arXiv:2411.07942 (Apple) — accuracy numbers + Irwin-Hall framing.
- arXiv:2506.17615 (EQuARX) — only paper with end-to-end speedup measurements for quantized AllReduce; validates "dequantize-between-hops" design as MSE 0.0014.
- arXiv:2412.04964 (Flash Communication) — perplexity numbers for INT4/INT6/INT8 TP comm.
- arXiv:2411.09510 (Yao et al.) — companion to the Apple paper on TP comm compression.
- OCP Microscaling Formats spec + arXiv:2310.10537 — MXINT8/MXFP8/MXFP4 format details.
- arXiv:1610.02132 (QSGD) — classical reference for stochastic-rounding all-reduce.
- arXiv:2411.09909 (AMXFP4) — asymmetric microscaling.
- main-horse blog post "Why reduction precision matters" — empirical observation that ring all-reduce stddev does **not** grow with N, supporting the channel-only design's robustness.

**Out of scope** (for this task):

- No CSL changes to `comm_pe.csl`.
- No simulator runs.
- No accuracy validation against real weights.
- No follow-on planning for the actual implementation — that becomes task 3 if and when the user approves moving forward.

**Success criteria:**

- The report convincingly distinguishes channel-quantization from accumulator-quantization, with citations.
- The method catalog covers all approaches the user named ("just include all the methods").
- The speedup table is reproducible from doc 1's numbers + the assumed bytes-proportional transit model, with the model's assumptions explicitly stated.
- The report can stand alone as a reference for a future implementation task.
